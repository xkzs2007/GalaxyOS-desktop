# GalaxyOS — 重型依赖（CUDA 版本，有 NVIDIA GPU 的本地环境用）
# 安装：pip install -r requirements-heavy-cuda.txt
#
# 前置条件：NVIDIA GPU + CUDA Toolkit 12.8+
# 无 GPU 请用：pip install -r requirements-heavy-cpu.txt
#
# Windows 注意：
#   - hnswlib 编译需要 MSVC Build Tools (winget install Microsoft.VisualStudio.2022.BuildTools)

# === 向量检索（GPU 加速）===
faiss-cpu>=1.7.0
# faiss-gpu 需要 CUDA，且版本兼容性严格，按需安装
# faiss-gpu>=1.7.0
hnswlib==0.8.0

# === 深度学习（CUDA 12.8）===
onnxruntime-gpu>=1.15.0
# torch CUDA 版本需单独安装：
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
transformers>=4.44.0

# === 数据分析 ===
pandas>=2.0.0

# ── torch 安装说明 ──
# CUDA 12.8: pip install torch --index-url https://download.pytorch.org/whl/cu128
# CUDA 12.6: pip install torch --index-url https://download.pytorch.org/whl/cu126
# CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118