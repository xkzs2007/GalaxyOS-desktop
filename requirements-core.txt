# GalaxyOS — 核心依赖（轻量，CI 必装）
# 安装：pip install -r requirements-core.txt
# 这层依赖 < 100MB，30 秒内装完

# === 数值计算（轻量）===
numpy>=2.0.0
scipy>=1.10.0

# === 进程间通信 ===
pyzmq>=25.0.0

# === 网络请求 ===
aiohttp>=3.9.0
httpx>=0.27.0
requests>=2.31.0

# === 图像处理 ===
Pillow>=10.0.0

# === 高速序列化 ===
orjson>=3.9.0

# === 数据处理 ===
polars>=1.0.0
duckdb>=1.0.0

# === 中文 NLP ===
jieba>=0.42.0
snownlp>=0.12.0
tiktoken>=0.5.0

# === 异步加速 ===
# Linux/macOS: uvloop; Windows: winloop（uvloop 的 Windows 原生替代）
uvloop>=0.19.0; sys_platform != "win32"
winloop>=0.6.0; sys_platform == "win32"

# === 系统监控 ===
psutil>=5.9.0

# === LLM API 调用 ===
openai>=1.0.0

# === 神经电路策略 ===
ncps>=1.0.0; python_version >= "3.9" and python_version < "3.13"

# === Tokenizer ===
tokenizers>=0.20.0

# === 数据校验 ===
pydantic>=2.0.0

# === 机器学习（轻量）===
scikit-learn>=1.3.0

# === MCP Server ===
fastmcp>=2.14.2,<3.0
starlette>=0.36.0
uvicorn>=0.29.0
sse-starlette>=2.0.0

# === OpenJiuwen Agent Core ===
openjiuwen>=0.1.13

# === OpenJiuwen Studio ===
# 注意: PyPI 版本锁死 openjiuwen==0.1.10，与 >=0.1.13 冲突
# CI 中从 agent-studio 源码 --no-deps 安装，不走 PyPI
# openjiuwen-studio>=0.1.5

# === 测试 ===
pytest>=9.0.0
pytest-cov>=5.0.0
