# GalaxyOS — 重型依赖（CI 可选，本地开发装）
# 安装：pip install -r requirements-heavy.txt
# 这层依赖 ~3GB（torch + CUDA + transformers），CI 中用 Docker 缓存

# === Intel 数学库（预编译包，libs/ 中有 x86_64 tar.gz）===
mkl==2026.0.0
tbb==2023.0.0

# === 向量检索 ===
faiss-cpu>=1.7.0
hnswlib==0.8.0

# === 深度学习 ===
onnxruntime>=1.15.0
torch>=2.0.0
transformers>=4.44.0

# === 数据分析 ===
pandas>=2.0.0
