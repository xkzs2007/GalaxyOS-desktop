# 小艺 Claw 系统 — Python 依赖（全集）
# 安装：pip install -r requirements.txt
#
# 分层策略（CI 友好）：
#   requirements-core.txt  — 轻量核心 (< 100MB, 30s)
#   requirements-heavy.txt — 重型 ML 依赖 (~3GB, 5-10min)
#
# CI 中只装 core；本地开发装全集；Docker 镜像预装 heavy。

-r requirements-core.txt
-r requirements-heavy.txt
