FROM node:22-bookworm AS gateway

WORKDIR /app

COPY _eval/PilotDeck/package.json _eval/PilotDeck/pnpm-lock.yaml ./
RUN corepack enable && pnpm install --frozen-lockfile

COPY _eval/PilotDeck/ .
RUN pnpm build

EXPOSE 3000

CMD ["node", "dist/src/cli/pilotdeck.js", "server"]